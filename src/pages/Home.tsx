import React from 'react';
import Hero from '../components/Hero';
import Background from '../components/Background';

const Home = () => {
    return (
        <Background>
            <Hero />
        </Background>
    );
};

export default Home;
